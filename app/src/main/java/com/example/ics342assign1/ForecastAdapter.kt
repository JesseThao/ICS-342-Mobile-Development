package com.example.ics342assign1

import androidx.recyclerview.widget.RecyclerView
import androidx.recyclerview.widget.RecyclerView.Adapter
/*import com.example.ics342assign1.ForecastAdapter.ForecastItemViewHolder
import com.example.ics342assign1.data.DayForecast

class ForecastAdapter (private val Dataset) : Adapter<ForecastItemViewHolder> {

    class ForecastItemViewHolder(
        private val binding: ForecastItemViewHolder
        ): RecyclerView.ViewHolder(binding.root) {

            fun bind(forecast: DayForecast) {
                val resources = binding.root.content.resources
                binding.high.text = resources.getString(R.string.high_temp, forecast.temp.max)
            }

        fun onCreateViewHolder(viewGroup: ViewGroup, viewType: Int) {

        }
        } */
